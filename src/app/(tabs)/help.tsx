import {ScrollView, Text, View} from "react-native";
import {globalStyles} from "@/styles/global";
import {MasterScreen} from "@/components/masterScreen";

export default function HelpScreen() {
    return <MasterScreen title="Help" />
}